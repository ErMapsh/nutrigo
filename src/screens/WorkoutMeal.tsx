import React, { memo } from 'react'
import { View } from 'react-native'
import { withTheme } from 'react-native-paper'

function WorkoutMeal() {
    return (
        <View>

        </View>
    )
}

export default memo(withTheme(WorkoutMeal))
